APP_ENV=dev
API_PREFIX=/api
DEBUG=true

# PostgreSQL (async)
SQL_DSN = "your_database_dsn_here"
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_ECHO=false

# JWT (para etapas siguientes)
JWT_SECRET=change_me
ACCESS_EXPIRES_MIN=60
REFRESH_EXPIRES_DAYS=7
